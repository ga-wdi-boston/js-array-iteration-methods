'use strict';

var reduce = function reduce(callback, initialValue) {
  // callback(previousValue, currentValue, index, array)

};

var product = function product(previousValue, currentValue, index, array) {

};

var max = function product(previousValue, currentValue, index, array) {

};

